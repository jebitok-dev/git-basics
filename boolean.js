// === ; equality operator
// !== ; not equal operator
// < ; less than operator
// > ; greater than operator
// >= ; less than or equal to operator
// >= ; greater than or equal to operator

let temp = 100

if (temp <= 32){
    console.log( 'It is freezing outside')
}

if (temp >= 110) {
    console.log('It is way too hot outside') ;
 } else {
     console.log('Weather not that bad')
 }

