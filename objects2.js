let data = {
    name: 'John',
    location: 'Berlin',
    profession: 'Designer'
};
// John is a designer and lives in Berlin
console.log(`$ {data.name} is a $ {data.profession} lives in $ {data.location} `)
console.log(data.name + ' is a ' + data.profession + ' and lives in ' + data.location);